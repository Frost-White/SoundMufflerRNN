[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "soundmuffler-backend"
version = "0.1.0"
requires-python = ">=3.11"
description = "Sound Muffler API"

[tool.setuptools.packages.find]
where = ["."]
include = ["app"]
